# B题第一问 氢燃料电池低温冷启动建模交接文档

> 日期：2026-09-23  
> 版本：v0.2 论文核验与实施版  
> 范围：问题1 一维单电池瞬态自冷启动模型建立及验证  
> 状态：建模方案已确定，数值求解与参数校准待实施

本文依据题目原文件、两个原始 Excel 附件及三篇论文，确定第一问的推荐模型、闭合关系、参数处理和验证路线。文档可独立用于后续建模、编程和论文撰写。当前已经完成文献核验与数据量级计算，尚未完成耦合方程求解、参数拟合或题目表1和表2的数值填报。文中给出的容量、电阻及热量是独立核算，不是模型验证结果。

**推荐主线：建立包含膜及两侧催化层含水量的一维分层气—水—冰—热模型，用有限速率相变描述冻结，用孔隙占据和催化层活性衰减描述冰影响，用催化层及膜的质子电阻描述脱水影响。先采用确定性模型，做跨初温验证和机制消融，再按证据增加膜内冻结或随机成核。**

## 0 执行摘要与模型结构

第一版正式模型应同时解释三个现象：加载电流增大导致电压下降，温升和膜水合促使电压恢复，低温水相变化可能造成冰堵或阳极脱水。模型主闭环如下。

```mermaid
flowchart LR
    J[输入电流密度 j(t)] --> R[反应物消耗与产水]
    R --> W[离聚物水 蒸气 液水 冰]
    W --> P[孔隙率与扩散系数]
    W --> K[膜和催化层质子电导率]
    P --> V[电池电压]
    K --> V
    V --> H[电化学产热]
    W --> L[相变潜热]
    H --> T[分层温度场]
    L --> T
    T --> W
    T --> P
    T --> K
    T --> V
```

推荐按下表冻结开发范围，避免第一版同时引入过多不可辨识参数。

| 模型层级 | 必须包含 | 暂不包含 | 升级条件 |
|---|---|---|---|
| V1 无冰基准 | 分层气体传输、动态离聚物水、PEM与CL质子电阻、双极板热容、电压四项分解 | 冰和随机成核 | 能解释低电流段与电压恢复的主要趋势 |
| V2 确定性冷启动主模型 | V1全部内容、有限速率结冰、液水迁移、冰占孔、活性面积修正、相变热 | 随机成核、完整膜内冻结 | 两工况完成跨初温验证，质量和能量守恒通过 |
| V3 结构敏感性模型 | 膜内冻结或成核延迟，两者分别加入 | 同时释放大量相变参数 | V2在合理参数范围内出现有结构的系统残差 |

当前最可靠的结论是：双极板蓄热必须计入；膜水与孔隙水必须分开；催化层脱水必须能通过质子电阻影响电压；冰体积分数只能作为模型内部预测量。随机成核、融冰参数和冰的真实空间分布尚不能由附件2直接验证。

## 1 本次阅读改变了哪些判断

| 原交接方案中的判断 | 本次结论 | 对实现的影响 |
|---|---|---|
| 一维厚度模型适合第一问 | 保留 | 不引入流道三维 CFD 和 MPL；题面明确不考虑 MPL |
| 先只做孔隙总水与冰，膜水可以低阶近似 | 加强 | 主模型应至少分辨 aCL、PEM、cCL 的含水量，正式版本采用一维分布 |
| 冰堵是重点失效机制 | 扩充 | 同时诊断阳极脱水；不能只用冰含量解释低电压 |
| 欧姆损失主要计算 PEM | 需补充 | 增加两侧 CL 的有效质子传导损失，否则阳极 CL 脱水可能无法影响电压 |
| 冰面积修正取指数3.5 | 保留为题设经验闭合 | 指数来自附件1；Jiao 2009 式(7)(8)实际使用液水和冰的线性遮蔽，不是3.5次幂 |
| 所有低温水过程先用同一个饱和蒸气压 | 需区分 | 过冷液水与冰有不同饱和蒸气压；题面 Buck 负温分支为冰面形式 |
| 温度与电压拟合后即可报告冰量 | 限制结论 | 冰量属于内部推断，需附参数及模型结构的不确定性区间 |
| 两工况基本是同一电流输入 | 修正 | 总电流一致，但表中电流密度不同，累计电荷差约2.78% |
| 膜内冻结可直接照搬原论文 | 不宜 | Jiao 2009 与 Yao 2020 对冰生成位置有不同处理，需作为结构对照 |

## 2 三篇论文分别提供什么依据

### 2.1 Jiao 与 Li 2009 提供可拆分的一维物理模块

原文为 *Three-dimensional multiphase modeling of cold start processes in polymer electrolyte membrane fuel cells*，DOI：[10.1016/j.electacta.2009.06.072](https://doi.org/10.1016/j.electacta.2009.06.072)。以下定位均为所提供 PDF 的实际页码。

- PDF第3—4页，表2、表3：气体、液水、冰、未冻结膜水、冻结膜水、电势和能量分开守恒；反应产水首先进入阴极离聚物水。
- PDF第4页，式(6)：气体扩散修正为 $D_{eff}=D\varepsilon_0^{1.5}(1-s_l-s_i)^{1.5}$。这里 $s_l,s_i$ 是孔隙饱和度。
- PDF第4—5页，式(7)(8)：反应遮蔽系数为 $1-s_l-s_i$，没有提供附件中的3.5次幂。
- PDF第4—6页，式(2)、(10)—(14)：液水渗透率、毛细压力和液水迁移，可以沿厚度方向降维。
- PDF第6—7页，表6、表7：相间传递必须作为成对源项进入不同状态；表4给出的相关速率常数为 $\mathrm{s^{-1}}$。附件中的无量纲标注不能原样代入这些速率方程。
- PDF第7页，式(19)—(24)：膜水扩散、平衡吸水、低温未冻结水上限、电渗拖曳和质子电导率。
- PDF第8页，式(26)、(33)：CL质子电导率修正为 $\omega^{1.5}\kappa$；膜水解吸成蒸气需要吸热，完整相变热不能只算正的凝结和冻结热。

可借用其守恒框架与若干闭合关系，不能照搬其几何、交换电流或热边界。其膜厚为178 μm，本题为12 μm；其交换电流采用体积量 $\mathrm{A\,m^{-3}}$，本题参考模型使用面积量 $\mathrm{A\,m^{-2}}$。二者直接替换会产生量纲错误。

### 2.2 Yao 等 2020 要求把阳极脱水纳入可解释范围

原文为 *Analysis of the Failure Modes in the Polymer Electrolyte Fuel Cell Cold-Start Process—Anode Dehydration or Cathode Pore Blockage*，DOI：[10.3390/en13010256](https://doi.org/10.3390/en13010256)。

- PDF第4—6页，式(4)—(6)、(10)—(19)：显式保留膜水、过冷液水和冰，并通过相间传递连接。
- PDF第6页，式(15)：冻结起始可以由与过冷水体积、温度和时间有关的成核概率描述；冻结发生以后才启动相应的结冰路径。
- PDF第6页：讨论膜水脱出并在多孔区域成冰，不能把它等同于整个 PEM 内的吸附水直接变成孔隙冰。
- PDF第8—10页：初始水含量较低、电流较大时，电渗拖曳可能超过回扩散，引起阳极 CL 脱水和电压迅速下降；初始水含量较高时，阴极孔隙堵塞更突出。

本文有低初始含水量 $\lambda_0=3$、干气入口和较高后期电流，因此脱水机制必须保留。但该论文使用的纯氧入口、几何、绝热边界及加载方式不同，不能直接把文中的0.205/0.210 A·cm⁻²分界、$\lambda=1.32$或0.35 V停机值当成本题阈值。

### 2.3 Luo 与 Jiao 2018 限定模型层级与验证边界

原文为 *Cold start of proton exchange membrane fuel cell*，DOI：[10.1016/j.pecs.2017.10.003](https://doi.org/10.1016/j.pecs.2017.10.003)。

PDF第16页第3.5节总结了一维模型对厚度方向主导过程的适用性；第17—19页第4.1节讨论膜水、过冷液水、相变和成核随机性，并说明不同实验与模型采用的物理假设并不完全一致。

这支持本题采用一维模型，但不支持“温度小于0 ℃时所有水立即冻结”的统一假设。它也不意味着必须把随机成核、微观孔径分布和三维流场同时加入第一版。现有数据只有两条单次冷启动记录，无法可靠辨识完整成核概率分布。

## 3 原题和数据重新核验后的关键事实

### 3.1 电流密度与面积应作为输入口径问题单独处理

重新读取附件2的数值和公式：两个工作表均有184个样本，范围0—36.6 s，间隔0.2 s。

| 量 | −20 ℃ | −25 ℃ |
|---|---:|---:|
| 表中累计电荷 $\int jdt$，C·cm⁻² | 6.4239 | 6.6022 |
| 法拉第累计产水，mg·cm⁻² | 0.5992 | 0.6158 |
| 电压最低点，V | 0.484 | 0.482 |
| 最低点时刻，s | 13.6 | 13.4 |
| 高负载 $I/j$ 的中位数，cm² | 310.5 | 303.0 |
| 末端电流密度，A·cm⁻² | 0.2990 | 0.30528 |

−25 ℃工作表E3:E186的公式均采用相应B列电流除以303；−20 ℃工作表E列为数值记录，不能简单认定为统一除以310。附件1的25 cm²与两者均不一致。

**计算主线采用各工作表自己的电流密度列。** 将总电流/303、总电流/310.5及总电流/25作为独立的输入口径对照。不得把面积设置为与交换电流、相变常数一起任意优化的变量。对照后需区分“保留参数仅改输入”和“改口径后重新拟合”，不能混用两类结果。

两组数据的总电流历史一致，不代表模型输入 $j(t)$ 一致。因此跨温度验证应使用各自真实记录的 $j(t)$，并补做统一 $j(t)$ 的模型对照，才能分离输入差异与温度效应。

### 3.2 一个直接约束水模型的容量计算

阴极催化层全部孔隙被冰占满时，其单位面积冰质量上限为

$$
M_{i,cCL}^{max}=\rho_i\varepsilon_{0,cCL}L_{cCL}
=920\times0.4207\times11.3\times10^{-6}
=0.0043736\ \mathrm{kg\,m^{-2}}
=0.43736\ \mathrm{mg\,cm^{-2}}.
$$

对应的产水电荷约为4.6887 C·cm⁻²。附件2中，两组累计电荷分别在约31.0 s和30.4 s的采样点超过这一容量，而此后电压仍保持较高水平。

这不是冰含量测量，却是对模型的硬约束：**“全部产水立即在cCL原地结冰、没有膜吸水和水迁出”的模型无法解释完整记录。** 多出的水必须由离聚物吸收、以液态滞留或迁往其他层、以蒸气排出等过程承担；已超过cCL总孔隙体积的冰不能靠数值裁剪消失。

Jiao式(22)在−20 ℃和−25 ℃给出未冻结膜水上限约7.31和6.54。以本题 $EW=1\ \mathrm{kg\,mol^{-1}}$、$\omega_{CL}=0.3$、$\lambda_0=3$计算，若PEM和两侧CL的离聚物都从初值升至该上限，可增加的水库存约0.273和0.225 mg·cm⁻²。这是均匀温度、全部可达时的容量估计，不代表几十秒内一定实现；它说明膜吸水容量与cCL冰容量处于同一量级，不能忽略。

### 3.3 双极板蓄热判断成立，但需保留其假设

按附件材料密度作为层的有效密度直接计算：

$$
C_{A,MEA}\simeq60.76,\qquad C_{A,2BP}=6066.72,
\qquad C_{A,total}\simeq6127.48\quad
\mathrm{J\,m^{-2}\,K^{-1}}.
$$

用原始 $j,V,T$ 做梯形积分，以各工况首点温度作为环境温度，忽略相变和气体焓流，反推面热容约为6121和6329 J·m⁻²·K⁻¹。与交接文档中的粗略值稍有差异，量级结论相同。

这仅是热容量核算，不能当成独立预测验证。正式模型必须用模型电压计算 $j(1.48-V_{sim})$。若材料密度解释为骨架真密度，应先做孔隙体积分数加权，不能把已经是层有效值的参数再乘一次固体体积分数。双极板占主导的判断不受这项小范围口径差异改变。

### 3.4 新发现的参考公式歧义

1. 原题水守恒式使用 $\partial_t m_w=+\partial_xN_w+S_w$，但同时将 $N_w$ 定义为负梯度扩散通量。若按同一正向通量理解会形成反扩散。统一采用 $\partial_t m+\partial_xN=S$，并写明符号约定。
2. 原题式(13)写 $\varepsilon_g=\varepsilon_0(1-\varepsilon_l)$，后文又把冰定义为相对控制体总体积的体积分数。为避免混用，主模型区分绝对体积分数与孔隙饱和度。
3. 原题Buck公式负温分支为冰面饱和压；若保留过冷液水，不能用这个冰面分支代表液面饱和压。
4. 原题用局部氧浓度计算Nernst电压，又设置浓差损失。可作为题设经验参考，但若重新推导传质损失，必须避免同一浓度降低重复扣减。
5. 初始记录已经有2 A负载，0.799 V不是开路电压。附件的0.95 V不能强行覆盖第一个有载数据点，也不能替代Nernst公式中的标准电势。

## 4 推荐的计算域与状态

质量传输域沿用五层：aGDL、aCL、PEM、cCL、cGDL。

热域采用七层：aBP、aGDL、aCL、PEM、cCL、cGDL、cBP。双极板只承担传热和蓄热，不定义气体或孔隙冰。第一问不加入10 mm端板；单电池外表面采用题设对流边界。

| 区域 | 主要求解状态 |
|---|---|
| aBP、cBP | $T$ |
| aGDL | $T,c_{H_2},m_v,m_l,m_i$ |
| aCL | 上述状态及 $\lambda_a$ |
| PEM | $T,\lambda$；膜内冻结对照版本另加 $m_f$ |
| cCL | $T,c_{O_2},m_v,m_l,m_i,\lambda_c$ |
| cGDL | $T,c_{O_2},m_v,m_l,m_i$ |

气相水、液水和冰均以单位多孔控制体总体积的质量计量。离聚物水单独定义为

$$
b=\omega\frac{\rho_{pem}M_w}{EW}\lambda,
\qquad \omega=1\text{（PEM）},\quad0.3\text{（CL）}.
$$

这是对原题“总水量”的有意细化：多孔区孔隙总水为 $m_p=m_v+m_l+m_i$，全水库存为 $b+m_p$；PEM中主要为吸附水，不把吸附水套入气液饱和分配。CL中从总体积水质量反算 $\lambda$时，不能遗漏 $\omega$。

定义

$$
\varepsilon_l=\frac{m_l}{\rho_l},\quad
\varepsilon_i=\frac{m_i}{\rho_i},\quad
s_l=\frac{\varepsilon_l}{\varepsilon_0},\quad
s_i=\frac{\varepsilon_i}{\varepsilon_0},\quad
\varepsilon_g=\varepsilon_0(1-s_l-s_i).
$$

始终检查 $m_v,m_l,m_i,b\ge0$ 和 $s_l+s_i\le1$。输出题目表格时用 $\max\varepsilon_i$，同时在机制图上给出 $\max s_i$。本题所有多孔层的干孔隙率均低于0.99，因此只检查绝对体积分数0.99不能识别物理堵塞；应另报告剩余孔隙和传质可行性，不能偷偷把题设0.99改成另一个定义。

## 5 主模型的守恒与闭合

### 5.1 反应物与电流分布

$$
\partial_t(\varepsilon_gc_k)+\partial_xN_k=S_k,
\qquad N_k=-D_{k,eff}\partial_xc_k,
$$

$$
D_{k,eff}=D_{k,ref}
\left(\frac{T}{298.15}\right)^{1.75}
\frac{101325}{p}\varepsilon_g^{1.5}.
$$

保留题设平均反应速率：aCL中 $S_{H_2}=-j/(2FL_{aCL})$，cCL中 $S_{O_2}=-j/(4FL_{cCL})$，其他区域为零。电流统一转为 A·m⁻²。

质子电流在aCL内从0线性升至 $j$，在PEM中等于 $j$，在cCL内从 $j$线性降至0。电渗拖曳只沿离聚物通路存在，不能在GDL中沿用 $i_m=j$。

气体外边界由摩尔分数给出。附件氧气质量分数0.233对应摩尔分数约0.2100，不直接把0.233代入 $c_{O_2}=yp/(RT)$。aCL/PEM和cCL/PEM界面设置对应反应气体零通量，不把氢气或氧气浓度跨膜连续。

### 5.2 四类水的成对源项

以下 $r_{ab}>0$ 表示水从状态a转向b，单位统一为 $\mathrm{kg\,m^{-3}\,s^{-1}}$；$r_{li}$为净冻结率，可为负。

$$
\begin{aligned}
\partial_t b+\partial_xN_b&=S_{gen}-r_{bv}-r_{bl}-r_{bi},\\
\partial_t m_v+\partial_xN_v&=r_{bv}-r_{vl}-r_{vi},\\
\partial_t m_l+\partial_xN_l&=r_{bl}+r_{vl}-r_{li},\\
\partial_t m_i&=r_{bi}+r_{vi}+r_{li}.
\end{aligned}
$$

$$
S_{gen}=\frac{M_wj}{2FL_{cCL}}\quad\text{仅在cCL}.
$$

CL采用上式；GDL去掉b及其交换项；PEM仅求b的输运，主版本暂不引入内部冻结。产水先进入cCL离聚物，随后交换到孔隙相，这与Jiao、Yao的模型思路一致。四式相加后，全部相变源项严格消失，仅剩反应产水和边界通量。不得又在孔隙水方程重复加入 $S_{gen}$。

水通量采用

$$
N_b=-\frac{\rho_{pem}M_w}{EW}\omega^{1.5}D_b(T,\lambda)\partial_x\lambda
+\frac{n_dM_wi_m}{F},\qquad n_d=\frac{2.5\lambda}{22},
$$

$$
N_v=-M_wD_{v,eff}\partial_xc_v,
\qquad c_v=\frac{m_v}{M_w\varepsilon_g}.
$$

气体扩散系数中已经包含孔隙修正，不能再把总体积蒸气质量梯度当作气相浓度梯度而重复计算孔隙效应。冰不迁移，不能给“液水+冰总和”使用水蒸气扩散系数，否则等效允许固态冰高速扩散。

$D_b$首先使用题目式(24)，并用Jiao式(19)/Luo式(25)做模型结构敏感性分析。原题多项式与综述分段相关式的适用范围不同，特别是脱水时的低 $\lambda$ 区间；需记录外推范围，不能把低含水处的预测视为精确材料定律。

### 5.3 有限速率相变的具体主方案

为保证可复现，第一版采用下述确定性闭合。它是借鉴文献后针对本题的简化，不应称为某篇论文的原模型。

设 $T_f=273.15\ \mathrm K$，$p_v=c_vRT$，气相余量 $g=1-s_l-s_i$，离聚物水容量系数 $C_b=\omega\rho_{pem}M_w/EW$。用 $\lambda_{eq}(a)$ 表示Jiao式(20)的吸水关系，首版取 $a=p_v/p_{sat,l}$，液水直接交换由单独的 $r_{bl}$表示。

$$
r_{bv}=k_{bv}C_b[\lambda-\lambda_{eq}(a)]g.
$$

该项允许双向传递，正值为解吸，负值为吸收。超出经验关系规定的活度区间时用已选定的饱和关系处理并报告，不将多项式无界外推。

气液交换采用

$$
r_{vl}=\begin{cases}
k_c\varepsilon_g\dfrac{M_w}{RT}(p_v-p_{sat,l}),&p_v\ge p_{sat,l},\\
k_e\varepsilon_0s_l\dfrac{M_w}{RT}(p_v-p_{sat,l}),&p_v<p_{sat,l}.
\end{cases}
$$

净冻结率采用

$$
r_{li}=k_fm_l\mathbf1_{T<T_f}-k_mm_i\mathbf1_{T>T_f}.
$$

这是有限时间冻结，不是瞬时把全部液水变成冰。工程实现可将指示函数在一个很窄的温度带内作单侧连续化，并检查带宽收敛；不增加未被数据支持的自由“过冷温度”。

气冰凝华采用

$$
r_{vi}=k_{vi}\varepsilon_g\frac{M_w}{RT}[p_v-p_{sat,i}]_+\mathbf1_{T<T_f}.
$$

膜水向孔隙冰的迁移只在CL中设置：

$$
r_{bi}=k_{bi}C_b[\lambda-\lambda_{sat}(T)]_+g\mathbf1_{T<T_f}.
$$

主方案先以Jiao式(22)作为 $\lambda_{sat}$ 的固定经验闭合，并明确它是从未冻结膜水上限借用的工程阈值：

$$
\lambda_{sat}(T)=\left[-1.304+0.01479T-3.594\times10^{-5}T^2\right]^{-1},
\qquad223.15\le T<273.15\ \mathrm K.
$$

CL膜水—液水交换可在无结冰分支采用

$$
r_{bl}=k_{bl}C_b\{[\lambda-\lambda_l^*]_+-\mathbf1_{m_l>0}[\lambda_l^*-\lambda]_+\},
\qquad\lambda_l^*=\lambda_{eq}(1)\simeq14,
$$

并在主方案的低温直接成冰分支关闭此项；过冷液水仍可通过气液交换存在并有限速率冻结。若需要持续的低温离聚物排出过冷液水，则增加明确的成核开关，联动切换 $r_{bl},r_{bi},r_{vi}$，不要把互斥假设下的源项无条件叠加。

**这一方案是无成核等待的确定性起点。** 它可能偏早结冰；若在合理参数内无法解释附件2的持续运行，应首先检验水迁移边界、膜容量和该假设，再引入成核延迟。不能任意降低所有结冰速率到近零来制造“成功”。

若采用题目局部气液平衡替代 $r_{vl}$，需在每个控制体联立求解蒸气饱和值和 $\varepsilon_g$：孔隙率依赖液水体积，不能把前一步固定孔隙率代入后就不校正。平衡分配只用于孔隙水，不用于膜水，并通过焓守恒处理瞬时相变热。

### 5.4 低温饱和压与膜冻结的两项结构不确定性

对过冷液水，使用液面饱和压 $p_{sat,l}$；对冰，使用冰面饱和压 $p_{sat,i}$。按题面两支Buck表达式分别计算，在−20 ℃时约为125.6和103.3 Pa，在−25 ℃时约为80.8和63.3 Pa。液面支在负温区用于描述过冷液水，应明确这一扩展。

Jiao式(22)在−20 ℃给出 $\lambda_{sat}\approx7.31$，Yao第10页示例给出约8.8；将题设Buck压比代入同一Springer吸水多项式则约为7.76。它们来自不同闭合，并非统一常数。主方案固定一套，其余作为结构敏感性分支，不混合后仍声称严格复现某篇原文。

对PEM内部冻结，主版本先不设独立冰库，但全程监控 $\lambda(x,t)-\lambda_{sat}(T)$。若PEM内持续明显超过该经验上限，必须运行有 $m_f$ 的扩展版本，而不能仅裁剪 $\lambda$。扩展版通过成对交换 $b\leftrightarrow m_f$ 守恒，电导率仅由未冻结水确定，膜内冰也不加入GDL/CL孔隙冰体积分数。初值 $\lambda_0=3$本身低于这些低温上限，不能在初始时刻把全部膜水冻结。

Jiao 2009主模型省略冰升华，不应把其理由扩大成“常压下冰不能升华”。本题干气边界存在升华驱动力，可在冰量敏感性分支加入由 $p_v-p_{sat,i}$ 驱动且受存冰量约束的逆过程。现有数据不足以再独立辨识升华速率。

### 5.5 液水迁移及边界必须写明

需要允许水离开cCL，故建议实现一维毛细迁移：

$$
N_l=-\rho_l\frac{K_l}{\mu_l}\partial_xp_l,
\qquad K_l=K_0s_l^4(1-s_i)^4,
\qquad p_l=p_g-p_c(s_l).
$$

$p_c$采用Jiao式(11)与题设接触角、渗透率；本题两个接触角均大于90°，选疏水分支。采用 $D_l=-K_l\mu_l^{-1}dp_c/ds_l$ 时检查其符号，避免把毛细扩散写成反扩散。原文表面张力相关式的给定范围从273.15 K开始，在负温区需作为近似外推并进行物性敏感性分析。

- CL/PEM：孔隙蒸气和液水零通量；离聚物水通量连续。
- CL/GDL：气体通量连续；液水以压力/毛细平衡和通量衔接，不能强制不同材料的液水饱和度连续。
- aCL/PEM/cCL：若视为同种离聚物且忽略界面阻力，使用 $\lambda$连续和 $N_b$连续，不强制体积基准不同的b连续。
- GDL/流道：干气边界 $p_v=0$，液水采用只允许向外排出的排液边界；主方案可取出口液压与流道气压相接。以零排液边界作敏感性对照。

流道湿度会受有限气体流量影响，而题目没有给出流量。$p_v=0$代表理想干气供给的边界近似；不应额外虚构一个已测流量。必要时引入气侧传质阻力的边界情景，但不优先再增加待拟合参数。

## 6 电压闭合的两项必要改进

### 6.1 冰活性衰减用无量纲比例

$$
\chi_i(x,t)=(1-s_i)^{3.5},\qquad
j_{0,eff}=j_0(T)\bar\chi_i.
$$

主版本可对cCL作体积平均获得 $\bar\chi_i$，并同时报告最小值和空间分布。不要将有量纲的比表面积直接乘本题已经按几何面积定义的 $j_0$。采用平均活性系数属于简化，后续若出现强局部梯度，可用固定总电流约束下的局部反应分配或一维电势方程替代。

指数3.5作为附件给定值固定，不与相变速率和扩散指数一起优化。Jiao的 $1-s_l-s_i$ 作为模型结构对照，不能既套一个遮蔽公式又在 $j$ 上重复除以相同面积系数。

法拉第产水始终由外部 $j$决定。有效活性下降提高极化，直至无法维持给定负载；不会在负载电流保持不变时自动按面积比例减少总产水。

### 6.2 必须让CL脱水实际进入电压计算

对各位置采用

$$
\kappa(T,\lambda)=(0.5139\lambda-0.326)
\exp\left[1268\left(\frac1{303.15}-\frac1T\right)\right],
\qquad \kappa_{CL,eff}=\omega^{1.5}\kappa.
$$

在题设均匀体积反应近似下，可利用耗散功率推导面积比等效电阻：

$$
R_{ion,A}=\int_{aCL\cup PEM\cup cCL}
\frac{\gamma(x)^2}{\kappa_{eff}(x)}dx,
\qquad \gamma(x)=\frac{i_m(x)}{j}.
$$

$$
\eta_{ohm}=j(R_{ion,A}+R_c),\qquad
R_c=10^{-6}\ \Omega\,\mathrm{m^2}.
$$

这是 $q''_{Joule}=\int i_m^2/\kappa_{eff}\,dx=j\eta_{ohm}$ 的等效化推导，不是把文献中的某一式直接抄入。PEM内 $\gamma=1$，CL内线性变化；若层内电导率均匀，每层CL贡献 $L_{CL}/(3\kappa_{CL,eff})$。如此保留均匀反应近似，又能让阳极CL局部脱水增加端电压损失。

按−20 ℃、初始 $\lambda=3$核算，PEM电阻约0.225 Ω·cm²，两侧CL的上述等效质子电阻合计约0.560 Ω·cm²。后者约为前者的2.49倍。在0.3 A·cm⁻²下对应约0.168 V的CL损失，但实际加载到该电流时温度和含水量已经变化，因此这不是对实验末端的电压预测。该量级说明CL水合与脱水有可能显著影响电压恢复趋势。

Springer关系在极低含水区不再可靠，甚至可给出非正电导率。发生这种情况应报告模型适用边界或脱水失效，不得只把电导率截成很小正数后继续声称有效预测。

### 6.3 浓差损失建议采用一致的浓度基准

保留题目四项结构

$$
V=E_{rev}-\eta_{act}-\eta_{ohm}-\eta_{con},
$$

但主模型建议令 $E_{rev}$以流道边界分压为基准，再由已解出的CL反应区浓度计算浓差损失。氧气部分可用

$$
\eta_{con,O_2}=\frac{RT}{4F}
\ln\frac{c_{O_2,ch}}{c_{O_2,rxn}},
$$

氢气有明显损失时加上 $RT/(2F)\ln(c_{H_2,ch}/c_{H_2,rxn})$。这样同一浓度降低只扣减一次。取反应区平均浓度属于单电压模型的近似；温度代表值及浓度平均方式必须固定。

另一种等价组织是在Nernst中使用局部分压，将这部分传质损失隐含其中，不再额外减同一项。若严格复现题目给出的“局部Nernst+经验 $j_{lim}$”形式，应单独标记为参考版本，与上述一致版本比较。

$j_{lim}$仍可作为供氧裕度诊断。均匀反应、常系数、气体准稳态时，以cCL/PEM侧最低浓度为零可推得

$$
j_{lim}=\frac{4Fc_{O_2,ch}}
{L_{cGDL}/D_{cGDL}+L_{cCL}/(2D_{cCL})}.
$$

系数1/2来自CL内分布消耗；若使用CL平均浓度，则相应系数为1/3，两者不能混用。非均匀状态用带反应通量权重的扩散阻力积分或单位电流辅助问题计算。上述极限只作准稳态诊断，瞬态可行性仍以原传输方程的非负浓度和可供反应物为准。不能把已经因传质降低的局部浓度再次当成入口储库浓度，重复计算极限电流。

## 7 热方程及潜热处理

主方案采用分层导热：

$$
C_{eff}(x,t)\partial_tT=\partial_x(k_{eff}\partial_xT)+S_{ec}+S_{phase},
\qquad S_{aux}=0.
$$

$$
\int_{\Omega_T}S_{ec}dx=j[1.48-V_{sim}].
$$

将总电化学热按归一化权重分配到MEA，第一版可使用MEA内均匀权重；不得因加入双极板把总热源乘大，也不再另外叠加一遍活化热和欧姆热。需要精细温度场时改用分解热源，但其积分必须与所采用的总功率约定一致。

产水首先进入离聚物b，并按液态水焓近似处理时，与前述源项一致的相变热为

$$
S_{phase}=L_v(r_{vl}-r_{bv})
+(L_v+L_f)r_{vi}+L_f(r_{li}+r_{bi}).
$$

PEM冻结扩展版本另加内部净冻结率对应的 $L_f r_{bf}$。膜水与液水交换因采用同一参考焓，首版不单独增加吸附热；这是可说明的近似。

该式保留“膜水解吸成蒸气吸热”，因此既可使用1.48 V液态水热中性电压，也可计算后续凝结和凝华，而不凭空重复产生汽化潜热。若把产水直接投入蒸气，必须相应改变热源基准或扣除汽化能。

每单位反应电荷的汽化潜热约相当于0.2332 V，冻结潜热约相当于0.03112 V。漏掉解吸吸热可能远比冻结潜热本身更严重。只考虑反应生成水全部冻结的上限，−20 ℃工况冻结潜热约2000 J·m⁻²，相对该工况 $\int j(1.48-V)dt\approx56057\ \mathrm{J\,m^{-2}}$约3.6%；实际值由水状态决定。

更稳健的后续实现是采用液水基准的焓变量，在内部焓中包含蒸气 $+L_vm_v$和冰 $-L_fm_i$，并配套相通量的焓通量。此时不再显式叠加同一潜热源。两种能量写法择一使用。简单显热式若省略水的显热携带项，应将省略的边界焓列入近似误差说明和能量审计。

按题目定义输出空间平均温度：

$$
\bar T_{domain}=\frac{\sum_i T_i\Delta x_i}{\sum_i\Delta x_i}.
$$

非均匀网格不能直接对节点温度做算术平均。主报告明确采用扩展单电池热域，同时输出MEA平均温度作口径敏感性；不拿热容加权温度替代题目要求的空间平均值。附件没有测温位置和传感器动态说明，不能通过自由选择平均方式来改善拟合。

## 8 参数取值与辨识安排

### 8.1 固定量和需要说明的量

优先固定题设几何、孔隙率、$\omega=0.3$、$\lambda_0=3$、$h=40$、$R_c$、$E_a=67000$、扩散孔隙指数1.5和冰覆盖指数3.5。膜当量质量1000 g·mol⁻¹换算为1 kg·mol⁻¹；它与论文中1100 kg·kmol⁻¹的书写口径不同。

| 闭合参数 | 可用依据 | 首版处理 |
|---|---|---|
| 冻结、融化速率 | Jiao表4为1 s⁻¹ | 用1 s⁻¹作开发基线；只尝试辨识冻结缩放，融化固定 |
| 气液转换 | 附件1给1.0、1.0，Jiao给出浓度差驱动形式 | 在选定质量源项中明确赋予s⁻¹，并记录这是量纲补全 |
| 膜水—液水 | 附件1给0.5 | 按选定b基准速率定义为0.5 s⁻¹的基线，声明归一化 |
| 膜水—冰 | 附件1给1 | 按CL体积基准闭合定义为1 s⁻¹的基线；不能等同于全膜瞬时冻结 |
| 蒸气—冰 | 附件1给0.0001 | 在选定浓度差形式中作 $10^{-4}\ \mathrm{s^{-1}}$ 基线，并与文献1 s⁻¹作结构情景比较 |
| 膜水—蒸气 | 附件1给0.001、1.0，未标方向 | 不能宣称方向已被原文确认；设置两种方向赋值的离散情景，并用Jiao双向1 s⁻¹作对照 |
| 未冻结水上限 | Jiao式(22)，Yao的另一闭合 | 主模型选Jiao经验式，其余作为模型结构不确定性 |

这里补全单位不等于证明附件本意。特别是CL体积基准中是否含 $\omega$，会改变相同数值速率的有效时间尺度；所有速率必须与b、$\lambda$的定义一起记录。对膜水—蒸气方向，开发默认可先取解吸0.001 s⁻¹、吸收1 s⁻¹，但必须明确为工作假设，并实际运行互换方向对照，不能仅靠这一选择给出确定的物理结论。

### 8.2 辨识顺序

1. 固定输入口径、热域、观测平均方式和相变结构。
2. 先做低电流阶段的电压量级检查，对 $j_{0,ref}$给出可行范围；不能先用温度经验修正项吞掉水状态误差。
3. 用全时段 $V,T$联合辨识至多2—3个有证据支撑的参数：$j_{0,ref}$、一个膜水交换或扩散缩放、一个结冰缩放。缩放参数并非全部必须释放。
4. 用加权灵敏度矩阵SVD和参数剖面判断哪些参数组合可区分。若结冰速率不敏感，就固定为文献基线并报告范围。
5. 热容首先由结构决定；只有残差和热量审计同时支持时才增加一个小范围热修正，而不是与所有水参数一起优化。

两条短时温度、电压曲线不能同时唯一确定所有相变通道、膜水扩散、成核概率和冰覆盖参数。参数拟合优度不能替代可辨识性证据。

## 9 验证设计及结果解释

采用−20 ℃校准、−25 ℃完整时间轨迹外部检验，再反向复核；最终可给出两工况联合估计，但必须把“联合拟合”和“留出工况验证”分开。不能随机打乱相邻时刻后声称独立验证。

目标函数为

$$
\mathcal L(\theta)=\sum_{d,n}\left[
\left(\frac{V_{d,n}^{sim}-V_{d,n}^{exp}}{s_V}\right)^2+
\left(\frac{\bar T_{d,n}^{sim}-T_{d,n}^{exp}}{s_T}\right)^2\right]
+\mathcal R(\theta).
$$

$s_V,s_T$是事先说明的观测权重尺度，不伪称已知传感器噪声；$\mathcal R$仅对采用了明确文献先验的参数设置。残差有时间相关性时，置信范围采用连续时间块或参数剖面，不将184点当成184次独立启动实验。

除RMSE、MAE、最大误差外，应检查初始有载电压、13—14 s低点、恒流平台上的恢复斜率、末端温升和不同阶段的残差模式。题目表格按0、5、10、15、20、25、30、35 s填报，内部拟合使用全部采样点。

温度的题目相对误差按其℃口径填写，并另报K/℃绝对误差；不能为使百分比更小，悄悄将相对误差分母换成绝对温度K。

**附件2只能直接检验低温阶段的电压和平均温度。** 数据末端仍未过0 ℃，不能从中辨识融化常数、验证完整自启动成功时刻，或宣称验证了冰的空间分布。冰量与失效模式应作为带条件和不确定性的内部预测。

## 10 数值实现的可行路线

采用分层有限体积法，CL、PEM加密。可从 BP/GDL/aCL/PEM/cCL/GDL/BP 的4/8/6/12/12/8/4个控制体起步，随后整体加密比较；这些数量是起始方案，不是已经证明收敛的网格。

热传导和扩散面系数使用距离加权调和平均；液水跨层以压力和毛细关系衔接。连续时间离散用后向欧拉起步，再采用BDF或MATLAB ode15s等刚性积分方法。时间步对齐电流输入变化，输入用分段线性插值，避免高阶插值造成超调。

干燥阴极GDL的扩散时间估计约 $L^2/D_{eff}=1.9\times10^{-3}$ s，显著小于0.2 s采样间隔，因此气体准稳态可作为加速版本。但当剩余气孔接近堵塞，扩散时间会明显增大；应先保留全瞬态参考解，或在堵塞附近切回瞬态，不能把准稳态假设推广到所有状态。

每步联合迭代流程：

1. 读取 $j(t)$，更新由水冰状态确定的孔隙率、导电率、扩散率和相变驱动力。
2. 隐式求解气体、离聚物水和孔隙水，按成对源项转移质量。
3. 计算有效活性、电阻、供氧裕度和电压。
4. 用模型电压及一致相变热更新温度。
5. 迭代至归一化残差收敛，检查状态界限、水质量和能量积分，再接受时间步。

不得每步先产生负水量，再无记录地裁成零。冻结使水体积膨胀，约束必须针对 $m_l/\rho_l+m_i/\rho_i$，不是 $(m_l+m_i)/\rho_l$。若缩小步长后仍需超过孔隙容量，说明该负载或相态假设不可行，不能通过继续积分获得虚假的“启动成功”。

固定均匀反应速率在强冰堵情况下可能不再合理。若负浓度总是首先出现在个别CL网格，应先验证数值收敛，再升级为局部反应分配或电势方程；主简化模型的停止事件要与真实装置必然失败的结论区分。

至少完成：冰源项关闭时的常温参考检查、无电流无源守恒检查、封闭水相转换质量检查、冻结/融化热量符号检查、法拉第累计产水检查、网格与时间步加密检查，以及全时段水/能量收支。将误差要求写成输出量的工程容差，不能只报告求解器残差小于 $10^{-6}$。

## 11 机制消融与不确定性

| 对照 | 保持相同的量 | 要回答的问题 |
|---|---|---|
| 固定 $\lambda$与动态 $\lambda$ | 输入、热域、其余参数 | 膜和CL水合是否解释电压恢复 |
| 只计PEM电阻与计入CL质子电阻 | 相同水状态 | 阳极脱水能否真正影响输出电压 |
| 冰只占孔与冰同时遮蔽活性 | 相同相变结构 | 传质损失和活性损失各占多少 |
| 不同 $\lambda_{sat}$闭合 | 同一输入和几何 | 冰量是否高度依赖未冻结水上限 |
| 不含膜内冻结与含膜内冻结 | 同一边界及源项约定 | 是否需要额外膜内冰状态 |
| 直接结冰与成核延迟 | 相同固定物性 | 确定性无等待模型是否偏早结冰 |
| 排液与不排液边界 | 同一内部模型 | 结论是否被未知排水条件主导 |

先保持参数不变做机制对照，再按同样参数预算重新拟合，分别说明两种结果。否则重新拟合可能用其他参数完全补偿被删去的机制。

若引入Yao式(15)的成核，可使用危险率积分

$$
H(t)=\int_0^t\int_{\Omega_l}C_0
\exp\left[-\frac{C_1}{T(T_f-T)^2}\right]dV_l\,dt,
\qquad P(\text{已成核})=1-e^{-H(t)}.
$$

该模型中的液水体积是物理体积，需要乘真实横截面积；25与约303 cm²的面积冲突会直接改变成核概率，不能再像确定性单位面积守恒那样绕开。网格细化时危险率应由物理体积积分保持一致，不对每格赋相同、与体积无关的冻结概率。随机版本用固定随机种子和重复模拟统计，不用单次恰好吻合的数据轨迹证明优越性。

本题没有重复冷启动实验，故随机成核只宜用于结构敏感性和预测带，不宜作为第一版主要拟合工具。电压与温度很接近但冰量差异很大的可行参数组，应全部保留在内部状态的不确定性范围中。

## 12 最终需要交付的第一问结果

正式求解完成后，至少包括：

1. 两工况电流密度输入、原始与模型温度/电压曲线、残差曲线。
2. 按题目格式填写表1和表2；冰量标为模型预测。
3. $\lambda_a,\lambda_{PEM},\lambda_c$、膜与CL电阻、cCL氧浓度、冰饱和度及绝对冰体积分数的时间变化。
4. 水库存分解：离聚物、液水、蒸气、冰、累计排水；与反应累计产水闭合。
5. 电压损失分解、反应热/相变热/散热/蓄热收支。
6. 独立温度工况检验、网格和步长收敛、关键机制消融、参数可辨识性及冰量预测区间。

低电压诊断同时查看 $\min\lambda_a$、$R_{aCL}$、$\min c_{O_2}$、$\max s_i$和 $\bar\chi_i$。脱水、冰堵和温升之间存在耦合，仅靠一次电压下降无法唯一辨认原因；本题数据未出现明确启动崩溃，更不应预先指定失效模式。

## 13 下一步实施顺序

第一步先写一个具有严格单位、分层边界和总量审计的无冰求解器，同时保留动态膜水及CL质子电阻。这一步已经可以检验电压先降后升是否主要来自加载、水合和温升。

第二步加入相变、液水迁移、冰占孔与活性修正，先运行附件参数及明确文献基线，不立即拟合。检查能否在36.6 s内保持物理可行，是否满足累计产水去向与热量约束。

第三步以少量参数进行跨初温验证；若拟合失败，根据残差和内部状态定位原因，再决定是水交换方向、排液边界、膜内冻结、成核等待还是局部反应分布需要升级。

**当前最值得优先投入的工作是“动态离聚物水 + CL质子传导 + 守恒相变”的主求解器。** 三篇文献已经足以为这一主线提供依据。继续堆积更多经验修正，或先开展第二问电堆优化，都不如先验证这条物理闭环。

### 13.1 实施检查清单

- [ ] 明确每个状态变量的单位、定义域和所在区域。
- [ ] 统一正向通量与源项符号，验证封闭系统总水守恒。
- [ ] 完成V1无冰基准，输出电压损失分解和离聚物水分布。
- [ ] 加入双极板热域，核对模型热量收支和空间平均温度口径。
- [ ] 完成V2相变模型，检查液水、冰和剩余气相孔隙率的可行性。
- [ ] 采用−20 ℃拟合、−25 ℃验证，并反向复核。
- [ ] 完成网格、时间步、输入面积口径和关键闭合关系的敏感性分析。
- [ ] 生成题目表1、表2及全部诊断图，区分实验量与模型内部预测量。
- [ ] 保存参数版本、代码版本、输入口径和随机种子，保证结果可复现。

## 来源及核算说明

- 题目：《氢燃料电池低温冷启动建模与控制策略研究.docx》，第一问、备注1及式(9)。原文公式为嵌入图像，本次已读取相应公式图像核对符号。
- 附件1：`参数清单`，包括几何、孔隙、膜、热物性和相变参数。
- 附件2：`-20℃`、`-25℃`，A3:E186；核对数值及原始公式，保留原文件不改动。
- Jiao K., Li X. *Electrochimica Acta*, 2009, 54: 6876–6891. DOI: 10.1016/j.electacta.2009.06.072。
- Luo Y., Jiao K. *Progress in Energy and Combustion Science*, 2018, 64: 29–61. DOI: 10.1016/j.pecs.2017.10.003。
- Yao L. et al. *Energies*, 2020, 13: 256. DOI: 10.3390/en13010256。

数值积分采用原始0.2 s记录的梯形积分。所有容量、电阻和量纲核验均为本文基于题设及明确公式重新计算。文献机制、为本题选取的闭合假设和已计算的量级应在正式论文中继续分开表述。

## 版本记录

### v0.2 2026-09-23

- 读取并核验Jiao与Li 2009、Luo与Jiao 2018、Yao等2020三篇论文。
- 将模型主线修订为动态离聚物水、孔隙水和冰的分层守恒模型。
- 增加阳极催化层脱水与催化层质子电阻的电压反馈。
- 区分绝对冰体积分数、孔隙冰饱和度、液面饱和压和冰面饱和压。
- 重新核验附件2累计电荷、法拉第产水、热容量和阴极催化层储冰容量。
- 明确确定性主模型、膜内冻结扩展和随机成核扩展的开发边界。
- 增加Markdown模型流程图、开发层级表和实施检查清单。
