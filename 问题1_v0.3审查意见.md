# 问题1 v0.3 方案审查意见

> 审查对象：《问题1_整合建模与MATLAB实施流程_v0.3.md》，对照 v0.2、《问题1_建模方案设计.md》、题目原文、附件1/2 及四篇论文。
> 结论：v0.3 结构严谨，守恒框架、源项矩阵、潜热符号、状态计数、网格与基准数字均已逐项复核无误，没有致命逻辑错误。下列问题按对结果的影响排序，前三项建议在写代码前修改。

## 已复核正确的部分

| 项目 | 复核结果 |
|---|---|
| 状态计数 62+18+24+42×3+30+12=272 | 正确 |
| 总长 4326.7 μm、MEA 326.7 μm、面热容 6127.48 | 正确 |
| 累计电荷 6.4239 / 6.6022 C/cm²，比值 1.0278 | 正确 |
| cCL 满冰 0.43736 mg/cm²、等价电荷 4.6887 C/cm² | 正确 |
| λ_sat(−20 ℃)=7.306，λ_sat(−25 ℃)=6.536 | 正确 |
| 冰面/液面饱和压数值 | 正确 |
| 转移矩阵 A_w 各列和为 0，方向与方程一致 | 正确 |
| 潜热式 S_phase 各项符号（结合水/液水为零焓参考） | 正确 |
| 能量审计 R_E 与热方程一致，不重复计相变热 | 正确 |
| CL 均匀反应的耗散等效电阻 L/(3κ)，Mao 式(26)为端点 L/(2κ) | 正确，1/3 是均匀反应下的标准结果 |
| U10 集总解（两侧散热 2h）、U12 最低氧浓度（系数 1/2）、平均浓度（1/3） | 正确 |
| κ_cold 的 100 倍为 S/cm→S/m 换算，前因子与 Springer 相同 | 正确 |
| 绝对冰体积分数不可能到 0.99（最大孔隙率 0.8） | 正确，问题2必须同时报告孔隙饱和度判据 |

v0.3 对旧方案（《问题1_建模方案设计.md》）的以下修正是正确的，应以 v0.3 为准：
- 旧方案漏了膜水解吸吸热 −L_v·r_bv。Jiao 原文式(33) 在 CL 内含 −h_cond·S_n−v，被忽略的只是吸附热与凝结热的差值。
- 旧方案建议的光滑正部 (z+√(z²+δ²))/2 在零驱动力时不为 0，会产生假相变。
- 旧方案预设末期 s_ice≈0.3–0.5、E_a≈35 kJ/mol，均属未经求解的推断。
- 旧方案"0.138 V 回升与实测一致"用的是 1/2 端点式；在 v0.3 的 1/3 主闭合下不成立（见问题6）。

---

## 高优先级（影响标定与验证结论）

### 1. −20 ℃工作表的 E 列存在严重舍入误差，两工况分用 E 列会引入虚假的电流差

复核结果：
- 两表电流列 B 完全相同，∫I dt 均为 2000.47 A·s。
- −25 ℃表 E=B/303（单元格公式）；−20 ℃表 E 为三位小数数值，∫I dt/∫E dt = 311.4。
- −20 ℃表 0–6.2 s 低电流段，E 只取 0.006 或 0.007，与 I/310 相比**单点误差最高 20.6%**。

后果：
- 低电流段恰好是辨识交换电流与活化能最干净的时间窗（见问题2）。20% 的输入误差对应活化过电位约 ±8 mV 的跳动，与要辨识的两温差信号（约 13 mV）同量级。
- 2.78% 的累计电荷差中约 2.4% 来自面积口径（303 与约 310），其余来自舍入。若两次实验是同一电池、同一电流程序，这个差值是记录口径造成的，会混入跨温度对比。

建议：
- 主输入改为 j(t)=I(t)/A，−20 ℃至少用 I/310 重建，去掉舍入误差。
- 更推荐两工况统一面积（以有公式可查的 303 cm² 为主），310 cm² 作为输入口径敏感性。v0.2 中"统一 j(t) 对照"在 v0.3 被删掉了，应恢复。
- 论文中说明：附件1的 25 cm² 与两者均不符，模型以面电流密度为输入。

### 2. 固定 E_a=67 kJ/mol 与最干净的数据窗口矛盾，且单温训练的跨温"预测"实质只在检验这个假设值

复核结果：
- 0–6 s 内 j≈0.0065 A/cm²：欧姆损失约 5 mV 且两温差 <0.5 mV，产水与温升可忽略（温度变化 <0.05 K）。电压几乎只由 E_rev(T) 与 η_act(T) 决定。
- 实测 0–6 s 平均电压：−20 ℃ 0.8003 V，−25 ℃ 0.8000 V，相差 0.3 mV。
- 按题设式(38)(39)(40)、α=0.5 计算两温电压差：

| E_a (kJ/mol) | j_0,ref (A/m²) | V(−20)−V(−25) |
|---|---|---|
| 67 | 0.01 | 11.5 mV |
| 67 | 0.25 | 14.2 mV |
| 40 | 0.25 | 4.9 mV |
| 26 | 0.25 | 0.0 mV |

逻辑问题：
- v0.3 第10.1节只标定 θ=[log j_0,ref, log f_CL]。用单一温度训练时，交换电流从 −20 ℃ 推到 −25 ℃ 的比例完全由固定的 E_a 决定。所以"−20 训练 → −25 预测"的误差主要反映 E_a=67 这个假设，而不是模型的预测能力。可以预见它会带来整段约 12–14 mV 的系统偏差。
- v0.3 说"两条曲线接近不能证明 E_a 应改成 35"。不能证明某个具体值是对的，但在这个时间窗里，67 kJ/mol 与数据的矛盾是清楚的，不需要借助其他机理。

建议：
- 在全模型标定前增加一个"低电流动力学子问题"：只用两工况 0–6 s 数据，以 T_ref=253.15 K 重参数化 j_0，辨识 (j_0(253 K), E_a)。计算量极小，参数相关性低。
- 主结果仍保留"E_a=67 题设值"一版，作为对照。
- 跨温度验证的表述改为"在给定 E_a 下的参数迁移检验"，避免把它称为预测能力验证。

### 3. 蒸气外边界取干燥 Dirichlet 是极端情形，它与吸/解吸方向（S1）强耦合，不能单因素对照

复核结果（−20 ℃，0.2 A/cm²）：
- cGDL 在干燥边界下的蒸气排出能力约 7.85×10⁻⁵ kg·m⁻²·s⁻¹，是产水速率（1.87×10⁻⁴）的 **42%**。
- 按 Mao & Wang 式(6) 以化学计量比 2 估算，流道蒸气排出上限只有产水的 **0.49%**。
- 两种边界相差近两个数量级。

逻辑问题：
- 主版本 k_des=0.001 时，cCL 解吸通量上限约为产水的 1.7%，蒸气通道被"关小"，边界假设看起来无关紧要。但这只是解吸系数的假设碰巧掩盖了边界问题。
- S1 互换为 k_des=1 后，cCL 大量水以蒸气从干燥边界排出，冰量大幅下降；阳极 aCL 持续向干氢解吸，脱水加快。S1 的结论几乎完全取决于边界是否干燥。
- 若 S1 在干燥边界下提前触发脱水事件，容易被误读为"数据否定了该方向"，其实是边界造成的。

建议：
- S1 与 S5 做 2×2 交叉，不做单因素对照。
- 主边界改为有限流量的流道湿度边界（假设化学计量比 2，与 Mao、Jiao 文献一致），干燥 Dirichlet 与零通量分别作为上下界。化学计量比是假设，需在论文中写明。

---

## 中优先级（影响口径或后续问题）

### 4. 替换题设浓差损失式后，附件1的"首尾/中间电池浓差极化系数（10/1）"失去落点

- v0.3 第6.3节以"通道分压 E_rev + 反应区浓度对数"替代题设式(43)(44)。在问题1的电流下两种写法都只有约 0.2 mV，数值上无关紧要。
- 附件1第23、24行给出首尾电池系数10、中间电池系数1，显然是乘在题设 η_con 上、用于问题2端部电池的。冰使 j_lim 骤降时，这个 10 倍会让端部电池先失效，这很可能正是出题人设计的端部失效机制。
- 建议：主模型保留题设式(43)(44)的结构（问题1系数取1），v0.3 的一致浓度版本作为对照。这样问题2可以直接接入该系数。

### 5. 平均温度口径必须在拟合前定死，并恢复 MEA 平均的敏感性

- v0.3 取七层空间平均，双极板占长度 92%，结果基本就是双极板温度。
- GDL 导热系数只有 0.3 W/(m·K)，反应热基本都要穿过 GDL 进入双极板储存。0.2–0.3 A/cm² 时 MEA 比双极板高约 0.2–0.5 K，与标定权重 s_T=0.5 K 同量级，约占摄氏相对误差的 3%。
- 题目"计算域"可理解为备注1的五层 MEA 域，也可理解为扩展后的七层热域，两种理解都说得通。v0.2 要求同时输出 MEA 平均，v0.3 删掉了。
- 建议：拟合前确定一种口径并在论文中写明理由，另一种作为必报敏感性。不能拟合后再挑。

### 6. 主闭合下电压回升的"预算"不够，应预期 f_CL<1

按 v0.3 主闭合（1/3 耗散等效、1268 K、ω^1.5），假设所有离聚物从 λ=3 水合到 λ_sat=7.31：

| CL 电阻写法 | f_CL | R(λ=3) Ω·cm² | R(λ=7.31) Ω·cm² | 0.2 A/cm² 下回升 |
|---|---|---|---|---|
| 1/3 耗散（主） | 1.0 | 0.786 | 0.278 | 0.101 V |
| 1/3 耗散（主） | 0.6 | 1.159 | 0.411 | 0.150 V |
| 1/2 端点（Mao） | 1.0 | 1.066 | 0.378 | 0.138 V |

- 实测 13.6→24 s 回升约 0.15 V，其中温升（2.7 K）贡献约 0.01 V。
- 上表是全部离聚物水合到上限的理论最大值。而电渗拖曳会使 aCL 脱水，实际回升更小。
- 结论：主闭合在 f_CL=1 时解释不了全部回升，标定大概率会把 f_CL 推到 0.6 以下，或需要 2222 K 电导率对照。f_CL 下界 0.3 目前够用，但若撞界要按 v0.3 第10.1节的规定排查。A1 消融实验的结论也依赖 f_CL 的取值。

---

## 低优先级（表述与实现细节）

### 7. Springer 吸附等温线系数的出处说反了
- v0.3 称 Mao–Wang–Tabuchi 式(13)"印为 17.18"，统一采用 Jiao 的 17.81。
- Springer 1991 原文通常引用为 **17.18**，Mao 等照录原文。Jiao 的 17.81 使 λ_eq(1)=14.003，与 a>1 分支 14+1.4(a−1) 连续；用 17.18 时 λ_eq(1)=13.373，在 a=1 处有 0.63 的跳变。
- 建议：继续用 17.81 没问题，但应表述为"为保证 a=1 处连续而采用的修正值（同 Jiao 2009）"，并引用 Springer 原式。不要把 17.18 称为误印。定稿前请核对 Springer 原文。

### 8. 膜水扩散多项式的适用范围说反了
- v0.3 第4.1节担心"含水量过大导致该经验多项式失效"。Springer 原关联式通常注明适用于 λ>4，本题初值 λ=3 恰好低于该范围。在本题 λ 范围内多项式恒为正（λ=0.65 时约 2.36），不会失效，但属于低含水外推。
- 建议：改为说明低含水外推，并纳入敏感性。

### 9. r_bi 中的 g 因子使接近堵孔时 CL 离聚物水"超饱和"
- r_bi = k_bi·C_m·[λ_nf−λ_sat]₊·g，当 s_i→1 时 g→0，冻结通道关闭。cCL 的 λ_nf 可以持续超过 λ_sat，电导率反而上升，与"非冻结水上限"的含义矛盾。
- Jiao 原式如此，但本方案要跑到接近堵孔的状态（问题2、3）。
- 建议：把 max(λ_nf−λ_sat) 列入诊断输出；必要时在 CL 内也设冻结离聚物水库存，或用 max(g, g_min) 保持冻结通道。

### 10. 有效导热系数未按附件1要求加权
- 附件1第19行明确要求有效导热系数"按固体、气体、液态水、冰等体积分数加权"，v0.3 用题设常数。
- 数值影响很小（温度梯度主要在 GDL，冰量有限），但属于附件明文要求，实现成本很低。建议实现，或在论文中说明影响小于网格误差。

### 11. 时间索引需带容差
- −25 ℃表时间列有 34.999999999999901、36.599999999999902 等浮点值。按 t==35 精确查找会失败。
- 建议：用 abs(t−t*)<1e−9，或 round(5t) 整数索引。

---

## 修改清单（建议写入 v0.4）

| 编号 | 修改 | 所在章节 |
|---|---|---|
| 1 | 输入改为 I/A；两工况统一面积为主，303/310 为敏感性；恢复统一 j(t) 对照 | 1.2、2.1、2.2 |
| 2 | 增加 0–6 s 低电流动力学子问题辨识 (j_0(253 K), E_a)；跨温检验改称"给定 E_a 下的参数迁移检验" | 1.2、10.1–10.3 |
| 3 | 主蒸气边界改为有限流量湿度边界（化学计量比 2 假设）；S1×S5 做 2×2 | 3.4、9.2 |
| 4 | 主模型保留题设式(43)(44)，预留首尾电池系数 | 6.3 |
| 5 | 平均温度口径拟合前定死，MEA 平均为必报敏感性 | 6.4、11.1 |
| 6 | 标注回升预算，预期 f_CL<1 | 10.1 |
| 7–11 | 表述与实现细节修正 | 4.1、5.2、11.2 |
